import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static Color background = const HSLColor.fromAHSL(1.0, 185, 0.30, 0.97).toColor();
  static Color foreground = const HSLColor.fromAHSL(1.0, 190, 0.40, 0.12).toColor();
  static Color card = Colors.white;
  static Color primary = const HSLColor.fromAHSL(1.0, 184, 0.65, 0.45).toColor();
  static Color primaryLight = const HSLColor.fromAHSL(1.0, 184, 0.80, 0.55).toColor();
  static Color primaryForeground = Colors.white;
  static Color secondary = const HSLColor.fromAHSL(1.0, 186, 0.25, 0.88).toColor();
  static Color accent = const HSLColor.fromAHSL(1.0, 12, 0.88, 0.65).toColor();
  static Color destructive = const HSLColor.fromAHSL(1.0, 0, 0.84, 0.60).toColor();
  static Color border = const HSLColor.fromAHSL(1.0, 186, 0.20, 0.88).toColor();
  static Color input = const HSLColor.fromAHSL(1.0, 186, 0.20, 0.90).toColor();
}